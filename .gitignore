# Baked-in build metadata (regenerated per build by tools/build_exe.py)
src/ql_console/_commit.py

# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Raw server dumps may contain secrets (rcon/stats passwords) — never commit
cvars.txt
cmds.txt
*.cvarlist
*.cmdlist
